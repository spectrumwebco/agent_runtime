# {{project_name}}

This project was bootstrapped with [Vite](https://vitejs.dev/) and Rumble.

## Available Scripts

In the project directory, you can run:

### `npm run dev`

Runs the app in the development mode.\
Open [http://localhost:5173](http://localhost:5173) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.

### `npm run build`

Builds the app for production to the `dist` folder.\
It correctly bundles React in production mode and optimizes the build for the best performance.

The build is minified and the filenames include the hashes.\
Your app is ready to be deployed!

### `npm run preview`

Locally preview the production build.

## Learn More

To learn more about the technologies used in this project:

- [Vite Documentation](https://vitejs.dev/guide/)
- [React Documentation](https://reactjs.org/)
{{#typescript}}
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)
{{/typescript}}

## Project Structure

```
{{project_name}}/
├── public/            # Static files
├── src/               # Source files
│   ├── assets/        # Assets like images, fonts, etc.
│   ├── components/    # React components
│   ├── App.{{#typescript}}tsx{{/typescript}}{{^typescript}}jsx{{/typescript}}        # Main App component
│   └── main.{{#typescript}}tsx{{/typescript}}{{^typescript}}jsx{{/typescript}}       # Entry point
├── .gitignore         # Git ignore file
├── index.html         # HTML template
├── package.json       # Project dependencies and scripts
{{#typescript}}
├── tsconfig.json      # TypeScript configuration
├── tsconfig.node.json # TypeScript configuration for Node
{{/typescript}}
└── vite.config.{{#typescript}}ts{{/typescript}}{{^typescript}}js{{/typescript}}    # Vite configuration
```

## Customization

You can customize this project by:

1. Adding new dependencies with `npm install`
2. Creating new components in the `src/components` directory
3. Modifying the styling in the CSS files
4. Updating the configuration in `vite.config.{{#typescript}}ts{{/typescript}}{{^typescript}}js{{/typescript}}`

## Deployment

This project can be deployed to various platforms:

- [Vercel](https://vercel.com/)
- [Netlify](https://www.netlify.com/)
- [GitHub Pages](https://pages.github.com/)
- [AWS Amplify](https://aws.amazon.com/amplify/)

For more information on deployment, refer to the [Vite deployment guide](https://vitejs.dev/guide/static-deploy.html).
